name: 'fintech_dbt_medallion'
version: '1.0.0'
config-version: 2

profile: 'fintech_dbt_medallion'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fintech_dbt_medallion:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table